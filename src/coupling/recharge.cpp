/**
 * @file recharge.cpp
 * @brief Recharge handling and vadose zone delay
 */

#include "dgw/coupling/recharge.hpp"

// RechargeHandler is header-only (all methods defined inline in recharge.hpp).
// This compilation unit ensures the header compiles cleanly.
